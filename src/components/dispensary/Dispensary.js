import React from 'react';
import Button from '../common/Button';

export const Dispensary = props => (
    <div>
        <h1>
            {props.display}
        </h1>

        <Button />

    </div>
);
